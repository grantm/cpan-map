#!/usr/bin/perl
#
# This script generates the JSON file and PNG map image used by the
# HTML/Javascript map frontend.
#

use strict;
use warnings;

use CPAN::Map::Builder;

CPAN::Map::Builder->generate(verbose => 1);

exit;

